# Vite build output (generated by `npm run build` in frontend/, outputs to repo-root dist/)
dist/

# Local compose environment (may hold the CARTO key) — commit .env.example only
.env
.env.*
!.env.example
.claude
.kiro